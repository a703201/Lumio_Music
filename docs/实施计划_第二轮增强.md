# 实施计划 · 第二轮增强

> 版本：v2.3.0 | 创建：2026-08-05

## 一、增强范围

| 类别 | 项数 | 说明 |
|------|------|------|
| 交互打磨 | F1~F7 | 长按菜单、详情面板、添加到歌单、年代解析、歌词滑动、真实封面、设置子页 |
| C++ 健壮性 | C1~C6 | UB 守卫、largesize、帧同步、标签扣除、Layer I、static_assert |
| NFR 尾项 | N1~N4 | NAPI 路径、类型声明、Logger、readFile |
| 错误处理 | E1~E3 | loadError、UI 错误态、空歌单守卫 |

## 二、实施任务

### F1 长按选项栏
- 五页落地：LocalLibrary / Favorites / PlayHistory / PlaylistDetail / Playlists
- `bindContextMenu(menu, ResponseType.LongPress)` 统一渲染
- 移除右侧 `ic_hm_more` 按钮

### F2 歌曲详情半模态面板
- `SongDetailSheet.ets`：文件名 / 标题 / 歌手 / 作曲家 / 合集 / 年代 / 添加时间
- `bindSheet` 高度 MEDIUM/LARGE
- 年份从音频元数据异步读取，不落 SongItem

### C1~C6 C++ 健壮性
- 全部在 `audio_metadata.cpp` 中修复
- 通过独立 harness 验证 3 个真实样本零回归

## 三、验收

- `bash build_hap.sh` BUILD SUCCESSFUL
- harmonyos-reviewer 0 ERROR / 0 WARNING
- 文档同步（README / CHANGELOG / PRD）

---

> 2026-08-06 更新：全部任务完成闭环。构建验证通过。
