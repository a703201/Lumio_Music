# 实施计划 · 第二轮增强（PRD 落地续作）

> 触发：用户要求按 PRD 继续完善 5 项，并在完成后做代码审查 + 更新文档
> 参考音频：`Bet On Me.flac` / `Lose My Mind.m4a` / `人是_.flac`（用于 C++ 解析器真实验证）
> 日期：2026-08-05

## 1. 目标与范围

| 编号 | 工作项 | 对应 PRD | 关键动作 |
|---|---|---|---|
| A | 歌单拖拽排序 | FR-24（原「明确排除」） | 数据层 `reorderPlaylistSongs` + 详情页 `List` 拖拽手势 |
| B | 非功能需求收尾 | §4 NFR | 空状态动画改 `animateTo` 循环（关 P2-1/2）；复核性能/安全/可维护 |
| C | C++ 原生后端计划项 | §5.3 To-Be | VBR 时长(P2-10) + 64-bit largesize(P2-9) + 真实音频验证 |
| D | 状态与存储风险收尾 | §5.4 | R1/R2/R3 已闭环；拖拽重排持久化无新冗余 |
| E | 下线发现页 | FR-21 | 删除孤儿文件 `Find.ets`（无导航/无引用） |

## 2. 架构与边界决策

- **C++ ↔ ArkTS 职责边界（明确）**：C++ `parseAudioMetadata` 仅作 MediaKit 失败/字段缺失时的**文本元数据兜底**（title/artist/album/duration/sr/channels）；封面/内嵌歌词仍由 ArkTS 层负责，避免重复解析与 I/O 浪费。
- **拖拽重排数据流**：`PlaylistDetail` 手势 → 解析 `onDrop` extraParams（dragIndex/insertIndex）→ `MusicStore.reorderPlaylistSongs`（权威 `songIds` 数组 splice + 持久化）→ 页面按 `songIds` 顺序重渲染。不引入新存储。

## 3. 实施结果（已实现，待审查）

- **A**：`MusicStore.reorderPlaylistSongs(playlistId, from, to)`；`PlaylistDetail` 列表项 `.onDragStart` 记录起点、`.onDrop` 调用重排；加「长按歌曲可拖动排序」提示。
- **B**：`Playlists.ets` 空状态呼吸动画由 `setInterval` 改为 `UIContext.animateTo` 循环（列表非空自动停止、无定时器残留）。
- **C**：`audio_metadata.cpp` 新增 Xing/Info VBR 时长与 64-bit largesize；standalone `g++` 验证 3 个真实文件，解析正确且零回归（见 `C++解析器真实音频验证.md`）。
- **D**：重排经 `savePlaylists` 持久化；三套存储机制边界维持上一轮结论，无新增冗余。
- **E**：`Find.ets` 已删除；`main_pages.json` 仅注册 `pages/Index`，无构建影响。

## 4. 风险与待办

- **沙箱无法出 HAP**：所有改动需在 DevEco 真机构建 + 真机复验（拖拽手势、MP3/VBR 样本、升级安装脏数据兜底）。
- **MP3/VBR 运行时验证缺口**：样本无 MP3 且环境无 ffmpeg，VBR 逻辑按规范实现但未跑真实 MP3（已登记进复验清单）。
- **审查驱动整改**：派 `code-reviewer` 对 5 项改动做静态合规审查，P0/P1 即时整改后回写 PRD/拆解表/CHANGELOG。

## 5. 交互打磨（第三轮，对应 CHANGELOG v2.3.0 / 2026-08-06）

> 本轮为交互与文档迭代，不动业务模型/持久化结构；全部改动已通过 `bash build_hap.sh` 构建（BUILD SUCCESSFUL，签名 HAP），并经 `harmonyos-reviewer` 审查 **0 ERROR / 0 WARNING**。
> 对应 PRD：FR-25 ~ FR-30；对应功能模块拆解表：模块 T（T-01~T-04）、K-05、I-01/I-02。

| 编号 | 工作项 | 对应 PRD / 拆解表 | 关键动作 | 状态 |
|---|---|---|---|---|
| F1 | 长按选项栏替代右侧「更多」按钮 | FR-25 / T-01 | 移除列表行 `ic_hm_more`，改 `bindContextMenu(menu, ResponseType.LongPress)` 长按唤出统一选项栏（`@Builder buildSongMenu` / `buildPlaylistMenu`），覆盖 LocalLibrary/Favorites/PlayHistory/PlaylistDetail/Playlists 五页，各页选项见 T-01 | ✅ 已实现 |
| F2 | 单一 `bindSheet` 分发（修复「详细」不弹） | FR-26/27 / T-02/T-03 / S-03 | 每页收敛为单一 `bindSheet($$this.sheetOpen, this.sheetContent(), {...})` + `@State sheetKind: 'detail'\|'add'\|'picker'` + `@Builder sheetContent()` 按 kind 渲染，消除多绑定后者覆盖前者导致面板不弹 | ✅ 已实现 |
| F3 | 歌曲详情半模态面板 | FR-26 / T-02 | 新增 `components/SongDetailSheet.ets`（`detents:[MEDIUM,LARGE]`、`preferType:BOTTOM`），展示 文件名/标题/歌手/作曲家/合集/年代/添加时间 | ✅ 已实现 |
| F4 | 添加到歌单半模态面板 | FR-27 / T-03 | 新增 `components/AddToPlaylistSheet.ets`（Medium），列出自建歌单一键加入（去重提示「歌曲已在歌单中」）、面板内新建歌单并立即加入 | ✅ 已实现 |
| F5 | 音频元数据「年代(year)」全链路解析 | FR-28 / T-04 / I-01 / I-02 | C++ `audio_metadata.cpp` 补 FLAC `DATE`/MP3 `TYER`+`TDRC`/MP4 `©day` → NAPI 返回 `year` → `AudioMeta.year`（MediaKit 优先路径从 `AVMetadata.dateTime` 经 `extractYear` 正则 `/(19\|20)\d{2}/` 抽 4 位）→ 详情面板异步显示；year 不落 `SongItem` | ✅ 已实现 |
| F6 | 歌词手动滑动浏览 | FR-29 / K-05 | `LrcView.ets` `onTouch` 状态机（`userOffsetY` 叠加偏移、`isUserScrolling` 控制清晰/模糊、`scheduleAutoReturn` 5 秒 `setTimeout` 回正） | ✅ 已实现 |
| F7 | 迷你播放器真实封面（一镜到底两端一致） | FR-30 / F-01 | `Layout.playerButton` 经 `CoverCache.getLabel()` 取正在播放歌曲真实内嵌封面（替代默认占位图），保留 `geometryTransition('player_cover', {follow:true})` 一镜到底 | ✅ 已实现 |

**构建与审查**
- 构建：`bash build_hap.sh` 前置 JBR、清空 `NODE_OPTIONS`/`BASH_ENV`、`--no-daemon`，稳定产出 `entry/build/default/outputs/default/entry-default-signed.hap`。
- 审查：`harmonyos-reviewer` **0 ERROR / 0 WARNING**（2 条 INFO 为 `componentSnapshot.get` 既有噪音），ArkTS 红线项（build 首语句、普通 `get` 访问器、裸 `console`/`hilog`、`ESObject` 赋值等）零复现。
- 文档同步：README / CHANGELOG / 功能模块拆解表已先行更新；本计划与 PRD §3.2（FR-25~30）、§5.2、§5.3、§6（M7）、§7.3 一并回写。

## 6. 后续补遗（2026-08-06）

> A~E + 第三轮 F1~F7 全部完成，以下为文档补遗。

- **构建已打通**：`build_hap.sh` 脚本稳定产出签名 HAP（BUILD SUCCESSFUL），沙箱无法出 HAP 的限制已解除。
- **新增组件/页面**：`CoverImageView.ets`（响应式封面组件，解决列表项复用时封面不刷新）、`SettingsCategory.ets`（设置子页）、`PrivacyPolicy.ets`（隐私政策页）。
- **`route_map.json`** 现为 **11 条**（新增 `SettingsCategory`）。
- **新增 `tools/` 目录**：4 个 Python 脚本用于 C++ 解析器与歌词解析器离线验证。
- **版权头统一**：全部源码文件添加 Apache-2.0 版权头。
- **PRD FR-31~FR-34** 已补录（设置子页/隐私政策/响应式封面/开发辅助脚本）。
