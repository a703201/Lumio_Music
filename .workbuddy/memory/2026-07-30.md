# 2026-07-30 工作日志

## 本轮"还债 + P0 + 已核实 API"落地完成（待 DevEco 验证）
- 技术债：删 `READ_MEDIA` 权限（确认全工程无媒体库访问），权限现仅 3 项（KEEP_BACKGROUND_RUNNING / INTERNET / GET_NETWORK_INFO）。
- P0 歌词：`LrcView.ets` 移除对测试框架 `@ohos/hypium` 的误 import，改本地 `const TAG`。
- P0 卡片：Form Kit 桌面播控卡片全套已建（`FormAbility.ets` + `form_config.json` + `WidgetCard.ets`），`AVSessionController.pushFormUpdate/remoteControl` 已接，`EntryAbility.handleControlWant` 已接，`module.json5` 注册 FormAbility + 字符串资源。
- 已核实 API：空间音频只读态（`Settings.ets` 调 `isSpatializationEnabledForCurrentDevice()`；`setSpatializationEnabled` 需系统权限，三方不可用，只展示）。
- 已核验：沙箱无法出 HAP（`--daemon` 起不来 / `--no-daemon` 卡 BuildNativeWithCmake 被 `[safe-delete]` 守卫拦），需用户在 DevEco 真机验证。
- 待办（下一波）：P0 剩余（权限引导/空态）、分享(systemShare)/设铃声(RingtoneKit, 需 mp3)/睡眠定时(建议应用内定时器)/小艺语音(insightIntent)/跨设备续播(continuationManager)。

## 播放队列 / 智能列表（已落地，待 DevEco 验证）
- 方案：复用现有播放列表底部弹窗（`ControlAreaComponent.musicListBuilder` + `MusicListContainer`），不新建页面，低风险。
- 引擎侧（`AudioRendererController`）新增队列权威管理：`getQueue/getCurrentIndex/setQueue(songs,startIndex,autoPlay)/removeFromQueue(index)/moveToPlayNext(index)` + 私有 `syncQueue()`（写回 AppStorage('songList'/'selectIndex') 并同步 `AVSessionController.setSongList(list, index)` 保证锁屏/卡片元数据与队列一致）。
- `AVSessionController` 新增 `setSongList(list, index?)`。
- 弹窗增强：排序栏（默认/标题/歌手/最近播放/随机 → `applySort`），行菜单（播放/下一首播放/移除 → 调引擎方法）；高亮改用引擎 `currentIndex`（随 onWillAppear/操作后 `refreshQueue` 刷新）；空态文案 `queue_empty`。
- 关键决策：弹窗展示仍读 `AppStorage('songList')`（原行为），避免"导入新歌后引擎队列不刷新导致弹窗缺歌"的回归；动作经引擎 + `syncQueue` 回写保持一致。
- 资源：`base/element/string.json` 新增 queue_*/sort_* 10 个键（工程仅 base，无 en/fr，无需多语言回退）。
- 已知遗留：引擎 `songList` 仅在构造时读 AppStorage，导入/删除歌曲后其 next/prev 仍是旧序（排序/移除动作会经 syncQueue 修正，但纯导入未触发）——属改动前既存的潜在问题，非本次回归，可作为后续快赢。
